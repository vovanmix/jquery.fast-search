#jQuery Plugin search autoComplete helper

Small and simple to use jQuery plugin to organize a search with autoComplete.
For examples of integration and server response please look in the `demo` directory   